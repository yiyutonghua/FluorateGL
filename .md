# 待处理事项

## 已知问题

- [ ] **glGetTexImage 模拟完善**:当前用 FBO + glReadPixels 模拟,但 GL_TEXTURE_3D / GL_TEXTURE_2D_ARRAY 的 layer 查询不精确,需补充 layer 参数处理
- [ ] **glGetIntegerv(GL_PARAMETER_BUFFER_BINDING) 查询**:GL_PARAMETER_BUFFER(0x80EE)的绑定查询(0x82B3)未在 translate_binding_to_desktop 中处理,返回原始 GLES ID(实际无 GLES buffer)

## 功能扩展

- [ ] **固定管线支持**:glBegin/glEnd、glVertex/glColor/glTexCoord 等立即模式 API(用户计划添加)
- [ ] **glPolygonMode 模拟**:GLES 只有 GL_FILL,需用 geometry shader 或 barycentric 模拟 GL_LINE/GL_POINT
- [ ] **glPixelStoref 模拟**:GLES 仅有 glPixelStorei,f 版本需包装
- [ ] **更多 GLES 不支持函数的模拟**:按需补充,遵循"先模拟,无模拟才 stub 占位"原则

## 性能优化

- [ ] **持久映射 buffer 的 fence 同步**:当前 draw 前 glBufferSubData 全量同步脏区域,可引入 GL_SYNC 对象实现更精细的同步,减少不必要的同步开销
- [ ] **Multi-draw 原生路径优先**:驱动支持 GL_EXT_multi_draw_indirect 时应优先用原生 glMultiDrawElementsIndirect 而非循环单次 Indirect(当前已实现,但需实测验证 Adreno 驱动支持情况)

## 测试与验证

- [ ] **Sodium 渲染验证**:1.21.1 fabric + sodium 进入世界后方块渲染正常(持久映射 buffer + IndirectCount 模拟已实现,待实测)
- [ ] **Adreno GPU 大视距崩溃**:视距拉到 8 后崩溃问题(历史记录,需确认是否已修复)
- [ ] **性能基准测试**:对比优化前后的帧率/帧时间
